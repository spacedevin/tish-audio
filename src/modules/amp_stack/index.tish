import { h } from "lattish"
import { PedalChassis, formatKnobLabel } from "../../moduleKit/ui/index.tish"
import { RotaryKnob } from "../../ui/controls.tish"
import { buildAmpSimAudio } from "../../moduleKit/audio/ampSimGraph.tish"

export const amp_stack_type = 'amp_stack';
export const amp_stack_category = 'AMP_SIM';

export const amp_stack_metadata = {
  label: 'Brit Stack',
  color: 'from-orange-600 via-amber-800 to-stone-950',
  theme: 'dark',
  defaultParams: {"gain":50,"bass":1,"mid":1,"treble":1,"presence":3000,"master":0.8}
};

export const amp_stack_layout = {"width":300,"height":340};

export fn amp_stack_View(props) {
  const { node, onRemove, onChange, onDragStart, onToggleBypass, previewOnly } = props;
  
  return (
    <PedalChassis
      node={node}
      onRemove={onRemove}
      onDragStart={onDragStart}
      onToggleBypass={onToggleBypass}
      previewOnly={previewOnly}
      panelW={amp_stack_layout.width}
      panelH={amp_stack_layout.height}
      isAmpSim={true}
      isCompact={false}
    >
      <RotaryKnob label={formatKnobLabel('gain')} min={0} max={100} value={node.params.gain} theme={node.theme} compact={true} onChange={(v) => onChange('gain', v)} resetValue={amp_stack_metadata.defaultParams.gain} />
      <RotaryKnob label={formatKnobLabel('bass')} min={0} max={5} value={node.params.bass} theme={node.theme} compact={true} onChange={(v) => onChange('bass', v)} resetValue={amp_stack_metadata.defaultParams.bass} />
      <RotaryKnob label={formatKnobLabel('mid')} min={-1} max={1} value={node.params.mid} theme={node.theme} compact={true} onChange={(v) => onChange('mid', v)} resetValue={amp_stack_metadata.defaultParams.mid} />
      <RotaryKnob label={formatKnobLabel('treble')} min={0} max={5} value={node.params.treble} theme={node.theme} compact={true} onChange={(v) => onChange('treble', v)} resetValue={amp_stack_metadata.defaultParams.treble} />
      <RotaryKnob label={formatKnobLabel('presence')} min={100} max={5000} value={node.params.presence} theme={node.theme} compact={true} onChange={(v) => onChange('presence', v)} resetValue={amp_stack_metadata.defaultParams.presence} />
      <RotaryKnob label={formatKnobLabel('master')} min={0} max={1} value={node.params.master} theme={node.theme} compact={true} onChange={(v) => onChange('master', v)} resetValue={amp_stack_metadata.defaultParams.master} />
    </PedalChassis>
  );
}

export fn amp_stack_buildAudio(ctx, node) {
  return buildAmpSimAudio(ctx, node, 800);
}
