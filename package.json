{
  "name": "tish-audio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist/assets && OUT_CSS=dist/assets/tish-tailwind.css tish run --feature full tish-tailwind/src/emit.tish && tish build --target js src/main.tish -o dist/main && cp index.html dist/index.html",
    "dev": "tish run --feature full dev-server.tish"
  },
  "dependencies": {
    "lattish": "file:../tish/lattish"
  }
}
